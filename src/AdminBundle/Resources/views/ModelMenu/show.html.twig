{% extends 'base.html.twig' %}

{% block body %}
    
    <div class="text-center"> 
    
    <h1>Модель Меню</h1>

    <table align="center" class="admin-table">
        <tbody>
            <tr>
                <th>Імя</th>
                <td>{{ modelMenu.name }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ modelMenu.id }}</td>
            </tr>
        </tbody>
    </table>

    <br/><a href="{{ path('modelmenu_index') }}">Вернутись назад</a><br/><br/>
    <a href="{{ path('modelmenu_edit', { 'id': modelMenu.id }) }}">Редагувати</a><br/><br/>
    
    <a class="btn btn-danger" href="{{ path('modelmenu_delete', {'id': modelMenu.id}) }}">Видалити</a>
    
            
   </div>
{% endblock %}
