{% extends 'base.html.twig' %}

{% block body %}
    
    <div class="text-center"> 
    
    <h1>Рубрика 1 перегляд</h1>

    <table align="center" class="admin-table table table-bordered table-hover h6">
        <tbody>
            <tr>
                <th class="table-success">Імя</th>
                <td>{{ OneRubric.name }}</td>
            </tr>
            <tr>
                <th class="table-success">Id</th>
                <td>{{ OneRubric.id }}</td>
            </tr>
        </tbody>
    </table>

    <br/><a href="{{ path('onerubric_index') }}" class="btn btn-outline-primary">Вернутись назад</a><br/><br/>
    <a href="{{ path('onerubric_edit', { 'id': OneRubric.id }) }}" class="btn btn-outline-primary">Редагувати</a><br/><br/>
    
    <a class="btn btn-outline-danger" href="{{ path('onerubric_delete', {'id': OneRubric.id}) }}">Видалити</a>
    
   <br/><br/>
   </div>
{% endblock %}
