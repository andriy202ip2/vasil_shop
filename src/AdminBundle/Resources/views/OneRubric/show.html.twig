{% extends 'base.html.twig' %}

{% block body %}
    
    <div class="text-center"> 
    
    <h1>Модель Меню</h1>

    <table align="center" class="admin-table">
        <tbody>
            <tr>
                <th>Імя</th>
                <td>{{ OneRubric.name }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ OneRubric.id }}</td>
            </tr>
        </tbody>
    </table>

    <br/><a href="{{ path('onerubric_index') }}">Вернутись назад</a><br/><br/>
    <a href="{{ path('onerubric_edit', { 'id': OneRubric.id }) }}">Редагувати</a><br/><br/>
    
    <a class="btn btn-danger" href="{{ path('onerubric_delete', {'id': OneRubric.id}) }}">Видалити</a>
    
            
   </div>
{% endblock %}
