{% extends 'base.html.twig' %}

{% block body %}
    <h1>Emale</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emale.id }}</td>
            </tr>
            <tr>
                <th>Emale</th>
                <td>{{ emale.emale }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('emale_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('emale_edit', { 'id': emale.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
