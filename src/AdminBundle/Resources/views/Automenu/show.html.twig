{% extends 'base.html.twig' %}

{% block body %}

    <div class="text-center"> 

        <h1>Авто Меню</h1>

        <table align="center" class="admin-table">
            <tbody>
                <tr>
                    <th>Модель</th>
                    <td>{{ autoMenu.model.name }}</td>
                </tr>
                <tr>
                    <th>Імя</th>
                    <td>{{ autoMenu.name }}</td>
                </tr>
                <tr>
                    <th>Id</th>
                    <td>{{ autoMenu.id }}</td>
                </tr>
            </tbody>
        </table>

        <br/><a href="{{ path('automenu_index') }}"><button class="ui-button ui-corner-all ui-widget">Вернутись назад</button></a><br/><br/>
        <a href="{{ path('automenu_edit', { 'id': autoMenu.id }) }}"><button class="ui-button ui-corner-all ui-widget">Редагувати</button></a><br/><br/>

        {{ form_start(delete_form) }}
            <button class="ui-button ui-corner-all ui-widget">Видалити</button>
        {{ form_end(delete_form) }}

    </div>
{% endblock %}
