{% extends 'base.html.twig' %}
{% block title %}Рубрика 2 перегляд{% endblock %}

{% block body %}

    <div class="text-center"> 

        <h1><span class="admin-sabrubric-color">Рубрика 1 &gt;</span> Рубрика 2 перегляд</h1>

        <table align="center" class="admin-table table table-bordered table-hover h6">
            <tbody>
                <tr>
                    <th class="table-success admin-widht-action2">Рубрика 1</th>
                    <td>{{ autoMenu.model.name }}</td>
                </tr>
                <tr>
                    <th class="table-success bloo">Рубрика 2 Імя</th>
                    <td class="delete-modal-info bloo">{{ autoMenu.name }}</td>
                </tr>
                <tr>
                    <th class="table-success">Id</th>
                    <td>{{ autoMenu.id }}</td>
                </tr>
            </tbody>
        </table>

        <br/><a href="{{ path('automenu_index') }}"><button class="btn btn-outline-primary">Вернутись назад</button></a><br/><br/>
        <a href="{{ path('automenu_edit', { 'id': autoMenu.id }) }}"><button class="btn btn-outline-primary">Редагувати</button></a><br/><br/>

        <a href="{{ path('automenu_delete', { 'id': autoMenu.id }) }}" class="btn btn-outline-danger is-delete">Видалити</a>
        <br/><br/><br/>
    </div>
{% endblock %}
