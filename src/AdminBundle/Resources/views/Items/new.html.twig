{% extends 'base.html.twig' %}
{% block title %}Запчастина створення{% endblock %}
{% block body %}
    
    <div class="text-center">
    
    <h1>Запчастина створення</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="text" name="no_submit" id="no_submit" value="2" style="display: none;">
        
        <br/><button class="btn btn-outline-primary">Створити</button>
    {{ form_end(form) }}

    <br/> <a href="{{ path('items_index') }}"><button class="btn btn-outline-primary">Вернутись назад</button></a><br/><br/><br/>
                
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        $(document).ready(function () {

            $(".mid, .aid").change(function () {
                $("#no_submit").val(1);
                $('form[name="shop_menubundle_items"]').submit();
            });
        });
    </script>   
{% endblock %}