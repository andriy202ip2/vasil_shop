{% extends 'base.html.twig' %}
{% block title %}Користувач редагування{% endblock %}
{% block body %}

    <div class="text-center">
        <h1>Користувач редагування</h1>

        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <br/><button class="btn btn-outline-primary">Редагувати</button>
        {{ form_end(edit_form) }}

        <br/> <a href="{{ path('user_index') }}"><button class="btn btn-outline-primary">Вернутись назад</button></a><br/><br/>

        <a href="{{ path('user_delete', { 'id': user.id }) }}" class="btn btn-outline-danger is-delete">Видалити</a>
        <br/><br/><br/>

    </div>
{% endblock %}
